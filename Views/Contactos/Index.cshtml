@model IEnumerable<TP2024_Etapa4_26012_24200_18577.Models.Contacto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoContactoTc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoContactoTc.Tcid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Coid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Coid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Coid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
